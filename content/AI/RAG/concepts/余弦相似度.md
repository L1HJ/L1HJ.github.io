- **公式**：
  $$
  \text{cosine\_sim}(x, y) = \frac{x \cdot y}{\|x\| \|y\|}
  $$

- **特点**：
  - 只关注向量的方向，不关心向量的长度  
  - 结果范围 \([-1, 1]\)，越接近 1 表示越相似  

- **适用场景**：
  - 文本相似度（TF-IDF、词向量）  
  - 推荐系统中的用户/物品向量匹配  
  - 高维稀疏向量